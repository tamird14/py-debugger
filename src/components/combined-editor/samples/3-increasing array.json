{
  "combinedCode": "\nclass Queue:\n    def __init__(self, initial_size: int):\n        self.arr=['_'] * initial_size\n        self.start = 0\n        self.end = 0\n\n    def enqueue(self, value: int):\n        print(f'adding {value}')\n        if self.end<len(self.arr):\n            self.arr[self.end] = value\n            self.end += 1\n        else:\n            double_arr = ['_'] * initial_size\n            for i, v in enumerate(self.arr):\n                double_arr[i] = v\n        #     self.arr = double_arr\n        # @viz\n        arr.cells=V('queue.arr')\n        print('inside')\n        # @end\n\n\nqueue = Queue(2)\n\n# @viz\n\nclass Button(Rect):\n    def __init__(self, on_click: callable, **kwargs):\n        super().__init__(**kwargs)\n        self._on_click = on_click\n\n    def on_click(self, x: int, y: int):\n        print('111')\n        self._on_click(x, y)\n\nclass LabeledButton(Panel):\n    def __init__(self, label: str, color: tuple[int, int, int], on_click: callable, **kwargs):\n        super().__init__(**kwargs)\n        self.add(\n            Button(on_click=on_click, \n                   width=kwargs.get('width', 1), height=kwargs.get('height', 1), color=color),\n            Label(label=label)\n        )\n\npanel = Panel(x=2, y=2)\ndef add_event(x: int, y: int):\n    print('222')\n    queue.enqueue('*')\narr = Array(cells=V('queue.arr'), x=2, y=2)\npanel.add(\n    arr,\n    LabeledButton(label='add', color=(150,100,150), on_click=add_event)\n)\n# @end\n\n",
  "textBoxes": []
}